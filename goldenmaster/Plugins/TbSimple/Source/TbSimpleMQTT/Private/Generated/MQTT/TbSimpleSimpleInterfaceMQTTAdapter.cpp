// SPDX-FileCopyrightText: Copyright ApiGear UG and Epic Games, Inc.
// SPDX-License-Identifier: MIT
// This file has been generated by the ApiGear Code Generator.
// Do not edit. Changes will be lost when the code is re-generated.

#include "TbSimple/Generated/MQTT/TbSimpleSimpleInterfaceMQTTAdapter.h"
#include "TbSimple/Generated/api/TbSimple.json.adapter.h"
#include "ApiGearMQTTHost.h"
#include "Engine/Engine.h"
#include "Misc/DateTime.h"
THIRD_PARTY_INCLUDES_START
#include <nlohmann/json.hpp>
THIRD_PARTY_INCLUDES_END

DEFINE_LOG_CATEGORY(LogTbSimpleSimpleInterfaceMQTTAdapter);
UTbSimpleSimpleInterfaceMQTTAdapter::UTbSimpleSimpleInterfaceMQTTAdapter()
{
}

void UTbSimpleSimpleInterfaceMQTTAdapter::Initialize(FSubsystemCollectionBase& Collection)
{
	Super::Initialize(Collection);
}

void UTbSimpleSimpleInterfaceMQTTAdapter::Deinitialize()
{
	if (Host.IsValid() && bIsConnected)
	{
		TArray<FString> TopicsArray;
		Topics.GetKeys(TopicsArray);
		Host.Get()->unsubscribeTopic(TopicsArray);
	}

	Super::Deinitialize();
}

TMap<FString, ApiGear::MQTT::FCallbackFunction> UTbSimpleSimpleInterfaceMQTTAdapter::createTopicMap()
{
	TMap<FString, ApiGear::MQTT::FCallbackFunction> Result;
	Result.Add(FString(TEXT("tb.simple/SimpleInterface/set/propBool")), [this](const IApiGearMqttClient::FReceivedMessage& InMsg)
		{
		OnSetPropBool(InMsg);
	});
	Result.Add(FString(TEXT("tb.simple/SimpleInterface/set/propInt")), [this](const IApiGearMqttClient::FReceivedMessage& InMsg)
		{
		OnSetPropInt(InMsg);
	});
	Result.Add(FString(TEXT("tb.simple/SimpleInterface/set/propInt32")), [this](const IApiGearMqttClient::FReceivedMessage& InMsg)
		{
		OnSetPropInt32(InMsg);
	});
	Result.Add(FString(TEXT("tb.simple/SimpleInterface/set/propInt64")), [this](const IApiGearMqttClient::FReceivedMessage& InMsg)
		{
		OnSetPropInt64(InMsg);
	});
	Result.Add(FString(TEXT("tb.simple/SimpleInterface/set/propFloat")), [this](const IApiGearMqttClient::FReceivedMessage& InMsg)
		{
		OnSetPropFloat(InMsg);
	});
	Result.Add(FString(TEXT("tb.simple/SimpleInterface/set/propFloat32")), [this](const IApiGearMqttClient::FReceivedMessage& InMsg)
		{
		OnSetPropFloat32(InMsg);
	});
	Result.Add(FString(TEXT("tb.simple/SimpleInterface/set/propFloat64")), [this](const IApiGearMqttClient::FReceivedMessage& InMsg)
		{
		OnSetPropFloat64(InMsg);
	});
	Result.Add(FString(TEXT("tb.simple/SimpleInterface/set/propString")), [this](const IApiGearMqttClient::FReceivedMessage& InMsg)
		{
		OnSetPropString(InMsg);
	});
	Result.Add(FString(TEXT("tb.simple/SimpleInterface/rpc/funcNoReturnValue")), [this](const IApiGearMqttClient::FReceivedMessage& InMsg)
		{
		OnInvokeFuncNoReturnValue(InMsg);
	});
	Result.Add(FString(TEXT("tb.simple/SimpleInterface/rpc/funcNoParams")), [this](const IApiGearMqttClient::FReceivedMessage& InMsg)
		{
		OnInvokeFuncNoParams(InMsg);
	});
	Result.Add(FString(TEXT("tb.simple/SimpleInterface/rpc/funcBool")), [this](const IApiGearMqttClient::FReceivedMessage& InMsg)
		{
		OnInvokeFuncBool(InMsg);
	});
	Result.Add(FString(TEXT("tb.simple/SimpleInterface/rpc/funcInt")), [this](const IApiGearMqttClient::FReceivedMessage& InMsg)
		{
		OnInvokeFuncInt(InMsg);
	});
	Result.Add(FString(TEXT("tb.simple/SimpleInterface/rpc/funcInt32")), [this](const IApiGearMqttClient::FReceivedMessage& InMsg)
		{
		OnInvokeFuncInt32(InMsg);
	});
	Result.Add(FString(TEXT("tb.simple/SimpleInterface/rpc/funcInt64")), [this](const IApiGearMqttClient::FReceivedMessage& InMsg)
		{
		OnInvokeFuncInt64(InMsg);
	});
	Result.Add(FString(TEXT("tb.simple/SimpleInterface/rpc/funcFloat")), [this](const IApiGearMqttClient::FReceivedMessage& InMsg)
		{
		OnInvokeFuncFloat(InMsg);
	});
	Result.Add(FString(TEXT("tb.simple/SimpleInterface/rpc/funcFloat32")), [this](const IApiGearMqttClient::FReceivedMessage& InMsg)
		{
		OnInvokeFuncFloat32(InMsg);
	});
	Result.Add(FString(TEXT("tb.simple/SimpleInterface/rpc/funcFloat64")), [this](const IApiGearMqttClient::FReceivedMessage& InMsg)
		{
		OnInvokeFuncFloat64(InMsg);
	});
	Result.Add(FString(TEXT("tb.simple/SimpleInterface/rpc/funcString")), [this](const IApiGearMqttClient::FReceivedMessage& InMsg)
		{
		OnInvokeFuncString(InMsg);
	});
	return Result;
}

void UTbSimpleSimpleInterfaceMQTTAdapter::setBackendService(TScriptInterface<ITbSimpleSimpleInterfaceInterface> InService)
{
	// unsubscribe from old backend
	if (BackendService != nullptr)
	{
		UTbSimpleSimpleInterfacePublisher* BackendPublisher = BackendService->_GetPublisher();
		checkf(BackendPublisher, TEXT("Cannot unsubscribe from delegates from backend service TbSimpleSimpleInterface"));
		if (!BackendPublisher)
		{
			return;
		}
		BackendPublisher->Unsubscribe(TWeakInterfacePtr<ITbSimpleSimpleInterfaceSubscriberInterface>(this));
	}

	// only set if interface is implemented
	checkf(InService.GetInterface() != nullptr, TEXT("Cannot set backend service - interface TbSimpleSimpleInterface is not fully implemented"));
	if (InService.GetInterface() == nullptr)
	{
		return;
	}

	// subscribe to new backend
	BackendService = InService;
	UTbSimpleSimpleInterfacePublisher* BackendPublisher = BackendService->_GetPublisher();
	checkf(BackendPublisher, TEXT("Cannot subscribe to delegates from backend service TbSimpleSimpleInterface"));
	if (!BackendPublisher)
	{
		return;
	}
	// connect property changed signals or simple events
	BackendPublisher->Subscribe(TWeakInterfacePtr<ITbSimpleSimpleInterfaceSubscriberInterface>(this));
}

void UTbSimpleSimpleInterfaceMQTTAdapter::setMQTTHost(TSoftObjectPtr<UApiGearMQTTHost> InHost)
{
	checkf(InHost.IsValid(), TEXT("Invalid pointer to MQTT host"));

	// remove old connection
	if (Host.IsValid())
	{
		TArray<FString> TopicsArray;
		Topics.GetKeys(TopicsArray);
		Host.Get()->unsubscribeTopic(TopicsArray);
	}
	Host = InHost;

	// set up new connection
	Host.Get()->OnConnectionStatusChanged().AddUObject(this, &UTbSimpleSimpleInterfaceMQTTAdapter::OnConnectionStatusChanged);

	// call right away, host could already be connected
	OnConnectionStatusChanged(Host.Get()->IsConnected());
}

void UTbSimpleSimpleInterfaceMQTTAdapter::OnConnectionStatusChanged(bool InIsConnected)
{
	bIsConnected = InIsConnected;
	if (!Host.IsValid() || !bIsConnected)
	{
		return;
	}

	// update topics map and subscribe
	Topics = createTopicMap();
	for (auto& Topic : Topics)
	{
		Host.Get()->subscribeTopic(Topic.Key, Topic.Value);
	}

	if (!BackendService)
	{
		return;
	}
	// push current property values to newly connected clients (retained)
	OnPropBoolChanged(BackendService->GetPropBool());
	OnPropIntChanged(BackendService->GetPropInt());
	OnPropInt32Changed(BackendService->GetPropInt32());
	OnPropInt64Changed(BackendService->GetPropInt64());
	OnPropFloatChanged(BackendService->GetPropFloat());
	OnPropFloat32Changed(BackendService->GetPropFloat32());
	OnPropFloat64Changed(BackendService->GetPropFloat64());
	OnPropStringChanged(BackendService->GetPropString());
}

void UTbSimpleSimpleInterfaceMQTTAdapter::OnInvokeFuncNoReturnValue(const IApiGearMqttClient::FReceivedMessage& InMsg)
{
	if (!BackendService)
	{
		UE_LOG(LogTbSimpleSimpleInterfaceMQTTAdapter, Error, TEXT("No backend service set for TbSimpleSimpleInterface - cannot handle FuncNoReturnValue invoke"));
		return;
	}
	const std::string PayloadStr(reinterpret_cast<const char*>(InMsg.Payload.GetData()), InMsg.Payload.Num());
	const nlohmann::json FunctionPayload = nlohmann::json::parse(PayloadStr);
	bool bParamBool = FunctionPayload.at(0).get<bool>();

	BackendService->FuncNoReturnValue(bParamBool);
}

void UTbSimpleSimpleInterfaceMQTTAdapter::OnInvokeFuncNoParams(const IApiGearMqttClient::FReceivedMessage& InMsg)
{
	if (!BackendService)
	{
		UE_LOG(LogTbSimpleSimpleInterfaceMQTTAdapter, Error, TEXT("No backend service set for TbSimpleSimpleInterface - cannot handle FuncNoParams invoke"));
		return;
	}
	if (InMsg.CorrelationData.Num() == 0 || InMsg.ResponseTopic.IsEmpty())
	{
		UE_LOG(LogTbSimpleSimpleInterfaceMQTTAdapter, Error, TEXT("OnInvokeFuncNoParams: missing CorrelationData or ResponseTopic"));
		return;
	}

	bool Result = BackendService->FuncNoParams();
	const nlohmann::json ResponsePayload = Result;
	Host.Get()->notifyInvokeResponseWithCorrelation(InMsg.ResponseTopic,
		UTF8_TO_TCHAR(ResponsePayload.dump().c_str()), InMsg.CorrelationData);
}

void UTbSimpleSimpleInterfaceMQTTAdapter::OnInvokeFuncBool(const IApiGearMqttClient::FReceivedMessage& InMsg)
{
	if (!BackendService)
	{
		UE_LOG(LogTbSimpleSimpleInterfaceMQTTAdapter, Error, TEXT("No backend service set for TbSimpleSimpleInterface - cannot handle FuncBool invoke"));
		return;
	}
	if (InMsg.CorrelationData.Num() == 0 || InMsg.ResponseTopic.IsEmpty())
	{
		UE_LOG(LogTbSimpleSimpleInterfaceMQTTAdapter, Error, TEXT("OnInvokeFuncBool: missing CorrelationData or ResponseTopic"));
		return;
	}
	const std::string PayloadStr(reinterpret_cast<const char*>(InMsg.Payload.GetData()), InMsg.Payload.Num());
	const nlohmann::json FunctionPayload = nlohmann::json::parse(PayloadStr);
	bool bParamBool = FunctionPayload.at(0).get<bool>();

	bool Result = BackendService->FuncBool(bParamBool);
	const nlohmann::json ResponsePayload = Result;
	Host.Get()->notifyInvokeResponseWithCorrelation(InMsg.ResponseTopic,
		UTF8_TO_TCHAR(ResponsePayload.dump().c_str()), InMsg.CorrelationData);
}

void UTbSimpleSimpleInterfaceMQTTAdapter::OnInvokeFuncInt(const IApiGearMqttClient::FReceivedMessage& InMsg)
{
	if (!BackendService)
	{
		UE_LOG(LogTbSimpleSimpleInterfaceMQTTAdapter, Error, TEXT("No backend service set for TbSimpleSimpleInterface - cannot handle FuncInt invoke"));
		return;
	}
	if (InMsg.CorrelationData.Num() == 0 || InMsg.ResponseTopic.IsEmpty())
	{
		UE_LOG(LogTbSimpleSimpleInterfaceMQTTAdapter, Error, TEXT("OnInvokeFuncInt: missing CorrelationData or ResponseTopic"));
		return;
	}
	const std::string PayloadStr(reinterpret_cast<const char*>(InMsg.Payload.GetData()), InMsg.Payload.Num());
	const nlohmann::json FunctionPayload = nlohmann::json::parse(PayloadStr);
	int32 ParamInt = FunctionPayload.at(0).get<int32>();

	int32 Result = BackendService->FuncInt(ParamInt);
	const nlohmann::json ResponsePayload = Result;
	Host.Get()->notifyInvokeResponseWithCorrelation(InMsg.ResponseTopic,
		UTF8_TO_TCHAR(ResponsePayload.dump().c_str()), InMsg.CorrelationData);
}

void UTbSimpleSimpleInterfaceMQTTAdapter::OnInvokeFuncInt32(const IApiGearMqttClient::FReceivedMessage& InMsg)
{
	if (!BackendService)
	{
		UE_LOG(LogTbSimpleSimpleInterfaceMQTTAdapter, Error, TEXT("No backend service set for TbSimpleSimpleInterface - cannot handle FuncInt32 invoke"));
		return;
	}
	if (InMsg.CorrelationData.Num() == 0 || InMsg.ResponseTopic.IsEmpty())
	{
		UE_LOG(LogTbSimpleSimpleInterfaceMQTTAdapter, Error, TEXT("OnInvokeFuncInt32: missing CorrelationData or ResponseTopic"));
		return;
	}
	const std::string PayloadStr(reinterpret_cast<const char*>(InMsg.Payload.GetData()), InMsg.Payload.Num());
	const nlohmann::json FunctionPayload = nlohmann::json::parse(PayloadStr);
	int32 ParamInt32 = FunctionPayload.at(0).get<int32>();

	int32 Result = BackendService->FuncInt32(ParamInt32);
	const nlohmann::json ResponsePayload = Result;
	Host.Get()->notifyInvokeResponseWithCorrelation(InMsg.ResponseTopic,
		UTF8_TO_TCHAR(ResponsePayload.dump().c_str()), InMsg.CorrelationData);
}

void UTbSimpleSimpleInterfaceMQTTAdapter::OnInvokeFuncInt64(const IApiGearMqttClient::FReceivedMessage& InMsg)
{
	if (!BackendService)
	{
		UE_LOG(LogTbSimpleSimpleInterfaceMQTTAdapter, Error, TEXT("No backend service set for TbSimpleSimpleInterface - cannot handle FuncInt64 invoke"));
		return;
	}
	if (InMsg.CorrelationData.Num() == 0 || InMsg.ResponseTopic.IsEmpty())
	{
		UE_LOG(LogTbSimpleSimpleInterfaceMQTTAdapter, Error, TEXT("OnInvokeFuncInt64: missing CorrelationData or ResponseTopic"));
		return;
	}
	const std::string PayloadStr(reinterpret_cast<const char*>(InMsg.Payload.GetData()), InMsg.Payload.Num());
	const nlohmann::json FunctionPayload = nlohmann::json::parse(PayloadStr);
	int64 ParamInt64 = FunctionPayload.at(0).get<int64>();

	int64 Result = BackendService->FuncInt64(ParamInt64);
	const nlohmann::json ResponsePayload = Result;
	Host.Get()->notifyInvokeResponseWithCorrelation(InMsg.ResponseTopic,
		UTF8_TO_TCHAR(ResponsePayload.dump().c_str()), InMsg.CorrelationData);
}

void UTbSimpleSimpleInterfaceMQTTAdapter::OnInvokeFuncFloat(const IApiGearMqttClient::FReceivedMessage& InMsg)
{
	if (!BackendService)
	{
		UE_LOG(LogTbSimpleSimpleInterfaceMQTTAdapter, Error, TEXT("No backend service set for TbSimpleSimpleInterface - cannot handle FuncFloat invoke"));
		return;
	}
	if (InMsg.CorrelationData.Num() == 0 || InMsg.ResponseTopic.IsEmpty())
	{
		UE_LOG(LogTbSimpleSimpleInterfaceMQTTAdapter, Error, TEXT("OnInvokeFuncFloat: missing CorrelationData or ResponseTopic"));
		return;
	}
	const std::string PayloadStr(reinterpret_cast<const char*>(InMsg.Payload.GetData()), InMsg.Payload.Num());
	const nlohmann::json FunctionPayload = nlohmann::json::parse(PayloadStr);
	float ParamFloat = FunctionPayload.at(0).get<float>();

	float Result = BackendService->FuncFloat(ParamFloat);
	const nlohmann::json ResponsePayload = Result;
	Host.Get()->notifyInvokeResponseWithCorrelation(InMsg.ResponseTopic,
		UTF8_TO_TCHAR(ResponsePayload.dump().c_str()), InMsg.CorrelationData);
}

void UTbSimpleSimpleInterfaceMQTTAdapter::OnInvokeFuncFloat32(const IApiGearMqttClient::FReceivedMessage& InMsg)
{
	if (!BackendService)
	{
		UE_LOG(LogTbSimpleSimpleInterfaceMQTTAdapter, Error, TEXT("No backend service set for TbSimpleSimpleInterface - cannot handle FuncFloat32 invoke"));
		return;
	}
	if (InMsg.CorrelationData.Num() == 0 || InMsg.ResponseTopic.IsEmpty())
	{
		UE_LOG(LogTbSimpleSimpleInterfaceMQTTAdapter, Error, TEXT("OnInvokeFuncFloat32: missing CorrelationData or ResponseTopic"));
		return;
	}
	const std::string PayloadStr(reinterpret_cast<const char*>(InMsg.Payload.GetData()), InMsg.Payload.Num());
	const nlohmann::json FunctionPayload = nlohmann::json::parse(PayloadStr);
	float ParamFloat32 = FunctionPayload.at(0).get<float>();

	float Result = BackendService->FuncFloat32(ParamFloat32);
	const nlohmann::json ResponsePayload = Result;
	Host.Get()->notifyInvokeResponseWithCorrelation(InMsg.ResponseTopic,
		UTF8_TO_TCHAR(ResponsePayload.dump().c_str()), InMsg.CorrelationData);
}

void UTbSimpleSimpleInterfaceMQTTAdapter::OnInvokeFuncFloat64(const IApiGearMqttClient::FReceivedMessage& InMsg)
{
	if (!BackendService)
	{
		UE_LOG(LogTbSimpleSimpleInterfaceMQTTAdapter, Error, TEXT("No backend service set for TbSimpleSimpleInterface - cannot handle FuncFloat64 invoke"));
		return;
	}
	if (InMsg.CorrelationData.Num() == 0 || InMsg.ResponseTopic.IsEmpty())
	{
		UE_LOG(LogTbSimpleSimpleInterfaceMQTTAdapter, Error, TEXT("OnInvokeFuncFloat64: missing CorrelationData or ResponseTopic"));
		return;
	}
	const std::string PayloadStr(reinterpret_cast<const char*>(InMsg.Payload.GetData()), InMsg.Payload.Num());
	const nlohmann::json FunctionPayload = nlohmann::json::parse(PayloadStr);
	double ParamFloat = FunctionPayload.at(0).get<double>();

	double Result = BackendService->FuncFloat64(ParamFloat);
	const nlohmann::json ResponsePayload = Result;
	Host.Get()->notifyInvokeResponseWithCorrelation(InMsg.ResponseTopic,
		UTF8_TO_TCHAR(ResponsePayload.dump().c_str()), InMsg.CorrelationData);
}

void UTbSimpleSimpleInterfaceMQTTAdapter::OnInvokeFuncString(const IApiGearMqttClient::FReceivedMessage& InMsg)
{
	if (!BackendService)
	{
		UE_LOG(LogTbSimpleSimpleInterfaceMQTTAdapter, Error, TEXT("No backend service set for TbSimpleSimpleInterface - cannot handle FuncString invoke"));
		return;
	}
	if (InMsg.CorrelationData.Num() == 0 || InMsg.ResponseTopic.IsEmpty())
	{
		UE_LOG(LogTbSimpleSimpleInterfaceMQTTAdapter, Error, TEXT("OnInvokeFuncString: missing CorrelationData or ResponseTopic"));
		return;
	}
	const std::string PayloadStr(reinterpret_cast<const char*>(InMsg.Payload.GetData()), InMsg.Payload.Num());
	const nlohmann::json FunctionPayload = nlohmann::json::parse(PayloadStr);
	FString ParamString = FunctionPayload.at(0).get<FString>();

	FString Result = BackendService->FuncString(ParamString);
	const nlohmann::json ResponsePayload = Result;
	Host.Get()->notifyInvokeResponseWithCorrelation(InMsg.ResponseTopic,
		UTF8_TO_TCHAR(ResponsePayload.dump().c_str()), InMsg.CorrelationData);
}

void UTbSimpleSimpleInterfaceMQTTAdapter::OnSigBoolSignal(bool bInParamBool)
{
	if (!Host.IsValid() || !bIsConnected)
	{
		return;
	}
	const nlohmann::json Args = {bInParamBool};
	const FString Topic = TEXT("tb.simple/SimpleInterface/sig/sigBool");
	Host.Get()->notifySignal(Topic, UTF8_TO_TCHAR(Args.dump().c_str()));
}

void UTbSimpleSimpleInterfaceMQTTAdapter::OnSigIntSignal(int32 InParamInt)
{
	if (!Host.IsValid() || !bIsConnected)
	{
		return;
	}
	const nlohmann::json Args = {InParamInt};
	const FString Topic = TEXT("tb.simple/SimpleInterface/sig/sigInt");
	Host.Get()->notifySignal(Topic, UTF8_TO_TCHAR(Args.dump().c_str()));
}

void UTbSimpleSimpleInterfaceMQTTAdapter::OnSigInt32Signal(int32 InParamInt32)
{
	if (!Host.IsValid() || !bIsConnected)
	{
		return;
	}
	const nlohmann::json Args = {InParamInt32};
	const FString Topic = TEXT("tb.simple/SimpleInterface/sig/sigInt32");
	Host.Get()->notifySignal(Topic, UTF8_TO_TCHAR(Args.dump().c_str()));
}

void UTbSimpleSimpleInterfaceMQTTAdapter::OnSigInt64Signal(int64 InParamInt64)
{
	if (!Host.IsValid() || !bIsConnected)
	{
		return;
	}
	const nlohmann::json Args = {InParamInt64};
	const FString Topic = TEXT("tb.simple/SimpleInterface/sig/sigInt64");
	Host.Get()->notifySignal(Topic, UTF8_TO_TCHAR(Args.dump().c_str()));
}

void UTbSimpleSimpleInterfaceMQTTAdapter::OnSigFloatSignal(float InParamFloat)
{
	if (!Host.IsValid() || !bIsConnected)
	{
		return;
	}
	const nlohmann::json Args = {InParamFloat};
	const FString Topic = TEXT("tb.simple/SimpleInterface/sig/sigFloat");
	Host.Get()->notifySignal(Topic, UTF8_TO_TCHAR(Args.dump().c_str()));
}

void UTbSimpleSimpleInterfaceMQTTAdapter::OnSigFloat32Signal(float InParamFloat32)
{
	if (!Host.IsValid() || !bIsConnected)
	{
		return;
	}
	const nlohmann::json Args = {InParamFloat32};
	const FString Topic = TEXT("tb.simple/SimpleInterface/sig/sigFloat32");
	Host.Get()->notifySignal(Topic, UTF8_TO_TCHAR(Args.dump().c_str()));
}

void UTbSimpleSimpleInterfaceMQTTAdapter::OnSigFloat64Signal(double InParamFloat64)
{
	if (!Host.IsValid() || !bIsConnected)
	{
		return;
	}
	const nlohmann::json Args = {InParamFloat64};
	const FString Topic = TEXT("tb.simple/SimpleInterface/sig/sigFloat64");
	Host.Get()->notifySignal(Topic, UTF8_TO_TCHAR(Args.dump().c_str()));
}

void UTbSimpleSimpleInterfaceMQTTAdapter::OnSigStringSignal(const FString& InParamString)
{
	if (!Host.IsValid() || !bIsConnected)
	{
		return;
	}
	const nlohmann::json Args = {InParamString};
	const FString Topic = TEXT("tb.simple/SimpleInterface/sig/sigString");
	Host.Get()->notifySignal(Topic, UTF8_TO_TCHAR(Args.dump().c_str()));
}

void UTbSimpleSimpleInterfaceMQTTAdapter::OnSetPropBool(const IApiGearMqttClient::FReceivedMessage& InMsg)
{
	if (!BackendService)
	{
		UE_LOG(LogTbSimpleSimpleInterfaceMQTTAdapter, Error, TEXT("No backend service set for TbSimpleSimpleInterface - cannot handle SetPropBool request"));
		return;
	}

	const std::string PayloadStr(reinterpret_cast<const char*>(InMsg.Payload.GetData()), InMsg.Payload.Num());
	const nlohmann::json JsonArgs = nlohmann::json::parse(PayloadStr);
	if (JsonArgs.empty())
	{
		return;
	}

	bool bPropBool = JsonArgs.get<bool>();
	BackendService->SetPropBool(bPropBool);
}

void UTbSimpleSimpleInterfaceMQTTAdapter::OnPropBoolChanged(bool bInPropBool)
{
	if (!Host.IsValid() || !bIsConnected)
	{
		return;
	}
	const FString Topic = TEXT("tb.simple/SimpleInterface/prop/propBool");
	Host.Get()->notifyPropertyChange(Topic, UTF8_TO_TCHAR(nlohmann::json(bInPropBool).dump().c_str()));
}

void UTbSimpleSimpleInterfaceMQTTAdapter::OnSetPropInt(const IApiGearMqttClient::FReceivedMessage& InMsg)
{
	if (!BackendService)
	{
		UE_LOG(LogTbSimpleSimpleInterfaceMQTTAdapter, Error, TEXT("No backend service set for TbSimpleSimpleInterface - cannot handle SetPropInt request"));
		return;
	}

	const std::string PayloadStr(reinterpret_cast<const char*>(InMsg.Payload.GetData()), InMsg.Payload.Num());
	const nlohmann::json JsonArgs = nlohmann::json::parse(PayloadStr);
	if (JsonArgs.empty())
	{
		return;
	}

	int32 PropInt = JsonArgs.get<int32>();
	BackendService->SetPropInt(PropInt);
}

void UTbSimpleSimpleInterfaceMQTTAdapter::OnPropIntChanged(int32 InPropInt)
{
	if (!Host.IsValid() || !bIsConnected)
	{
		return;
	}
	const FString Topic = TEXT("tb.simple/SimpleInterface/prop/propInt");
	Host.Get()->notifyPropertyChange(Topic, UTF8_TO_TCHAR(nlohmann::json(InPropInt).dump().c_str()));
}

void UTbSimpleSimpleInterfaceMQTTAdapter::OnSetPropInt32(const IApiGearMqttClient::FReceivedMessage& InMsg)
{
	if (!BackendService)
	{
		UE_LOG(LogTbSimpleSimpleInterfaceMQTTAdapter, Error, TEXT("No backend service set for TbSimpleSimpleInterface - cannot handle SetPropInt32 request"));
		return;
	}

	const std::string PayloadStr(reinterpret_cast<const char*>(InMsg.Payload.GetData()), InMsg.Payload.Num());
	const nlohmann::json JsonArgs = nlohmann::json::parse(PayloadStr);
	if (JsonArgs.empty())
	{
		return;
	}

	int32 PropInt32 = JsonArgs.get<int32>();
	BackendService->SetPropInt32(PropInt32);
}

void UTbSimpleSimpleInterfaceMQTTAdapter::OnPropInt32Changed(int32 InPropInt32)
{
	if (!Host.IsValid() || !bIsConnected)
	{
		return;
	}
	const FString Topic = TEXT("tb.simple/SimpleInterface/prop/propInt32");
	Host.Get()->notifyPropertyChange(Topic, UTF8_TO_TCHAR(nlohmann::json(InPropInt32).dump().c_str()));
}

void UTbSimpleSimpleInterfaceMQTTAdapter::OnSetPropInt64(const IApiGearMqttClient::FReceivedMessage& InMsg)
{
	if (!BackendService)
	{
		UE_LOG(LogTbSimpleSimpleInterfaceMQTTAdapter, Error, TEXT("No backend service set for TbSimpleSimpleInterface - cannot handle SetPropInt64 request"));
		return;
	}

	const std::string PayloadStr(reinterpret_cast<const char*>(InMsg.Payload.GetData()), InMsg.Payload.Num());
	const nlohmann::json JsonArgs = nlohmann::json::parse(PayloadStr);
	if (JsonArgs.empty())
	{
		return;
	}

	int64 PropInt64 = JsonArgs.get<int64>();
	BackendService->SetPropInt64(PropInt64);
}

void UTbSimpleSimpleInterfaceMQTTAdapter::OnPropInt64Changed(int64 InPropInt64)
{
	if (!Host.IsValid() || !bIsConnected)
	{
		return;
	}
	const FString Topic = TEXT("tb.simple/SimpleInterface/prop/propInt64");
	Host.Get()->notifyPropertyChange(Topic, UTF8_TO_TCHAR(nlohmann::json(InPropInt64).dump().c_str()));
}

void UTbSimpleSimpleInterfaceMQTTAdapter::OnSetPropFloat(const IApiGearMqttClient::FReceivedMessage& InMsg)
{
	if (!BackendService)
	{
		UE_LOG(LogTbSimpleSimpleInterfaceMQTTAdapter, Error, TEXT("No backend service set for TbSimpleSimpleInterface - cannot handle SetPropFloat request"));
		return;
	}

	const std::string PayloadStr(reinterpret_cast<const char*>(InMsg.Payload.GetData()), InMsg.Payload.Num());
	const nlohmann::json JsonArgs = nlohmann::json::parse(PayloadStr);
	if (JsonArgs.empty())
	{
		return;
	}

	float PropFloat = JsonArgs.get<float>();
	BackendService->SetPropFloat(PropFloat);
}

void UTbSimpleSimpleInterfaceMQTTAdapter::OnPropFloatChanged(float InPropFloat)
{
	if (!Host.IsValid() || !bIsConnected)
	{
		return;
	}
	const FString Topic = TEXT("tb.simple/SimpleInterface/prop/propFloat");
	Host.Get()->notifyPropertyChange(Topic, UTF8_TO_TCHAR(nlohmann::json(InPropFloat).dump().c_str()));
}

void UTbSimpleSimpleInterfaceMQTTAdapter::OnSetPropFloat32(const IApiGearMqttClient::FReceivedMessage& InMsg)
{
	if (!BackendService)
	{
		UE_LOG(LogTbSimpleSimpleInterfaceMQTTAdapter, Error, TEXT("No backend service set for TbSimpleSimpleInterface - cannot handle SetPropFloat32 request"));
		return;
	}

	const std::string PayloadStr(reinterpret_cast<const char*>(InMsg.Payload.GetData()), InMsg.Payload.Num());
	const nlohmann::json JsonArgs = nlohmann::json::parse(PayloadStr);
	if (JsonArgs.empty())
	{
		return;
	}

	float PropFloat32 = JsonArgs.get<float>();
	BackendService->SetPropFloat32(PropFloat32);
}

void UTbSimpleSimpleInterfaceMQTTAdapter::OnPropFloat32Changed(float InPropFloat32)
{
	if (!Host.IsValid() || !bIsConnected)
	{
		return;
	}
	const FString Topic = TEXT("tb.simple/SimpleInterface/prop/propFloat32");
	Host.Get()->notifyPropertyChange(Topic, UTF8_TO_TCHAR(nlohmann::json(InPropFloat32).dump().c_str()));
}

void UTbSimpleSimpleInterfaceMQTTAdapter::OnSetPropFloat64(const IApiGearMqttClient::FReceivedMessage& InMsg)
{
	if (!BackendService)
	{
		UE_LOG(LogTbSimpleSimpleInterfaceMQTTAdapter, Error, TEXT("No backend service set for TbSimpleSimpleInterface - cannot handle SetPropFloat64 request"));
		return;
	}

	const std::string PayloadStr(reinterpret_cast<const char*>(InMsg.Payload.GetData()), InMsg.Payload.Num());
	const nlohmann::json JsonArgs = nlohmann::json::parse(PayloadStr);
	if (JsonArgs.empty())
	{
		return;
	}

	double PropFloat64 = JsonArgs.get<double>();
	BackendService->SetPropFloat64(PropFloat64);
}

void UTbSimpleSimpleInterfaceMQTTAdapter::OnPropFloat64Changed(double InPropFloat64)
{
	if (!Host.IsValid() || !bIsConnected)
	{
		return;
	}
	const FString Topic = TEXT("tb.simple/SimpleInterface/prop/propFloat64");
	Host.Get()->notifyPropertyChange(Topic, UTF8_TO_TCHAR(nlohmann::json(InPropFloat64).dump().c_str()));
}

void UTbSimpleSimpleInterfaceMQTTAdapter::OnSetPropString(const IApiGearMqttClient::FReceivedMessage& InMsg)
{
	if (!BackendService)
	{
		UE_LOG(LogTbSimpleSimpleInterfaceMQTTAdapter, Error, TEXT("No backend service set for TbSimpleSimpleInterface - cannot handle SetPropString request"));
		return;
	}

	const std::string PayloadStr(reinterpret_cast<const char*>(InMsg.Payload.GetData()), InMsg.Payload.Num());
	const nlohmann::json JsonArgs = nlohmann::json::parse(PayloadStr);
	if (JsonArgs.empty())
	{
		return;
	}

	FString PropString = JsonArgs.get<FString>();
	BackendService->SetPropString(PropString);
}

void UTbSimpleSimpleInterfaceMQTTAdapter::OnPropStringChanged(const FString& InPropString)
{
	if (!Host.IsValid() || !bIsConnected)
	{
		return;
	}
	const FString Topic = TEXT("tb.simple/SimpleInterface/prop/propString");
	Host.Get()->notifyPropertyChange(Topic, UTF8_TO_TCHAR(nlohmann::json(InPropString).dump().c_str()));
}
