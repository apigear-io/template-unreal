// SPDX-FileCopyrightText: Copyright ApiGear UG and Epic Games, Inc.
// SPDX-License-Identifier: MIT
// This file has been generated by the ApiGear Code Generator.
// Do not edit. Changes will be lost when the code is re-generated.
#pragma once

#include "Counter/Generated/api/AbstractCounterCounter.h"
#include "Containers/Map.h"
#include "HAL/CriticalSection.h"
#include "Async/Future.h"
#include "Runtime/Launch/Resources/Version.h"
#include "Templates/PimplPtr.h"
#include "ApiGearMQTTTypes.h"
#include "ApiGearConnection.h"
#include "Subsystems/GameInstanceSubsystem.h"
#include "Misc/Guid.h"
#include "CounterCounterMQTTClient.generated.h"

class CounterCounterPropertiesData;
DECLARE_LOG_CATEGORY_EXTERN(LogCounterCounterMQTTClient, Log, All);

UCLASS(NotBlueprintable, BlueprintType)
class COUNTERMQTT_API UCounterCounterMQTTClient : public UAbstractCounterCounter
{
	GENERATED_BODY()
public:
	explicit UCounterCounterMQTTClient();

	// only needed in 4.25 to use TUniquePtr<CounterCounterPropertiesData>
	UCounterCounterMQTTClient(FVTableHelper& Helper);
	virtual ~UCounterCounterMQTTClient();

	// subsystem
	void Initialize(FSubsystemCollectionBase& Collection) override;
	void Deinitialize() override;

	// properties
	FCustomTypesVector3D GetVector() const override;
	void SetVector(const FCustomTypesVector3D& Vector) override;

	FVector GetExternVector() const override;
	void SetExternVector(const FVector& ExternVector) override;

	TArray<FCustomTypesVector3D> GetVectorArray() const override;
	void SetVectorArray(const TArray<FCustomTypesVector3D>& VectorArray) override;

	TArray<FVector> GetExternVectorArray() const override;
	void SetExternVectorArray(const TArray<FVector>& ExternVectorArray) override;

	// operations
	FVector Increment(const FVector& Vec) override;
	TFuture<FVector> IncrementAsync(const FVector& Vec) override;

	TArray<FVector> IncrementArray(const TArray<FVector>& Vec) override;
	TFuture<TArray<FVector>> IncrementArrayAsync(const TArray<FVector>& Vec) override;

	FCustomTypesVector3D Decrement(const FCustomTypesVector3D& Vec) override;
	TFuture<FCustomTypesVector3D> DecrementAsync(const FCustomTypesVector3D& Vec) override;

	TArray<FCustomTypesVector3D> DecrementArray(const TArray<FCustomTypesVector3D>& Vec) override;
	TFuture<TArray<FCustomTypesVector3D>> DecrementArrayAsync(const TArray<FCustomTypesVector3D>& Vec) override;

	UFUNCTION(BlueprintCallable, Category = "ApiGear|Counter|Counter")
	void UseConnection(TScriptInterface<class IApiGearConnection> InConnection);

	/**
	 * Used when the interface client changes subscription status:
	 * either is linked(ready to use) with server side (true) or it is in unlinked state (false).
	 */
	UPROPERTY(BlueprintAssignable, Category = "ApiGear|Counter|Counter|Remote", DisplayName = "Subscription Status Changed")
	FApiGearRemoteApiSubscriptionStatusChangedDelegateBP _SubscriptionStatusChangedBP;
	FApiGearRemoteApiSubscriptionStatusChangedDelegate _SubscriptionStatusChanged;

	/**
	 * Informs about the subscription state of the interface client.
	 * @return true if the client is subscribed (plugged in the network) and ready to send and receive messages or false if the server cannot be reached.
	 */
	UFUNCTION(BlueprintCallable, Category = "ApiGear|Counter|Counter|Remote")
	bool _IsSubscribed() const;

private:
	void onConnectionStatusChanged(bool bIsConnected);

	/// @brief factory to create the topic map which is used for bindings
	/// @return map with all topics and corresponding function callbacks
	TMap<FString, ApiGear::MQTT::FCallbackFunction> createTopicMap(const FString& ClientId);

	/// @brief sets the value for the property Vector coming from the service
	/// @param InMsg contains the param of the type FCustomTypesVector3D
	void setVectorLocal(const IApiGearMqttClient::FReceivedMessage& InMsg);

	/// @brief sets the value for the property ExternVector coming from the service
	/// @param InMsg contains the param of the type FVector
	void setExternVectorLocal(const IApiGearMqttClient::FReceivedMessage& InMsg);

	/// @brief sets the value for the property VectorArray coming from the service
	/// @param InMsg contains the param of the type TArray<FCustomTypesVector3D>
	void setVectorArrayLocal(const IApiGearMqttClient::FReceivedMessage& InMsg);

	/// @brief sets the value for the property ExternVectorArray coming from the service
	/// @param InMsg contains the param of the type TArray<FVector>
	void setExternVectorArrayLocal(const IApiGearMqttClient::FReceivedMessage& InMsg);

	/// @brief publishes the value for the signal ValueChanged coming from the service
	/// @param InMsg contains the param(s) of the type(s) const FCustomTypesVector3D& Vector, const FVector& ExternVector, const TArray<FCustomTypesVector3D>& VectorArray, const TArray<FVector>& ExternVectorArray
	void onValueChanged(const IApiGearMqttClient::FReceivedMessage& InMsg);

	/// @brief callback for invoke replies with return value
	/// @param InMsg contains the result payload and MQTT 5 CorrelationData identifying the call
	void onInvokeReply(const IApiGearMqttClient::FReceivedMessage& InMsg);

	// member variable to store the last sent data
	TPimplPtr<CounterCounterPropertiesData> _SentData;
	TScriptInterface<class IApiGearConnection> Connection;
	TMap<FString, ApiGear::MQTT::FCallbackFunction> Topics;

	// subscription readiness tracking
	bool bIsSubscribed = false;
	bool bAnySubscribeFailed = false;
	int32 PendingSubscriptions = 0;
	void DecrementPendingSubscription(bool bSuccess);

	// response handler map for RPC replies with return values
	TMap<FGuid, ApiGear::MQTT::InvokeReplyFunc> ResponseHandlerMap;
	FCriticalSection ResponseHandlerMutex;
	FGuid RegisterResponseHandler(ApiGear::MQTT::InvokeReplyFunc&& Handler);
};
