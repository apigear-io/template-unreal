// SPDX-FileCopyrightText: Copyright ApiGear UG and Epic Games, Inc.
// SPDX-License-Identifier: MIT
// This file has been generated by the ApiGear Code Generator.
// Do not edit. Changes will be lost when the code is re-generated.
#pragma once

#include "TbNames/Generated/api/AbstractTbNamesNamEs.h"
#include "Containers/Map.h"
#include "HAL/CriticalSection.h"
#include "Async/Future.h"
#include "Runtime/Launch/Resources/Version.h"
#include "Templates/PimplPtr.h"
#include "ApiGearMQTTTypes.h"
#include "ApiGearConnection.h"
#include "Subsystems/GameInstanceSubsystem.h"
#include "Misc/Guid.h"
#include "TbNamesNamEsMQTTClient.generated.h"

class TbNamesNamEsPropertiesData;
DECLARE_LOG_CATEGORY_EXTERN(LogTbNamesNamEsMQTTClient, Log, All);

UCLASS(NotBlueprintable, BlueprintType)
class TBNAMESMQTT_API UTbNamesNamEsMQTTClient : public UAbstractTbNamesNamEs
{
	GENERATED_BODY()
public:
	explicit UTbNamesNamEsMQTTClient();

	// only needed in 4.25 to use TUniquePtr<TbNamesNamEsPropertiesData>
	UTbNamesNamEsMQTTClient(FVTableHelper& Helper);
	virtual ~UTbNamesNamEsMQTTClient();

	// subsystem
	void Initialize(FSubsystemCollectionBase& Collection) override;
	void Deinitialize() override;

	// properties
	bool GetSwitch() const override;
	void SetSwitch(bool bSwitch) override;

	int32 GetSomeProperty() const override;
	void SetSomeProperty(int32 SomeProperty) override;

	int32 GetSomePoperty2() const override;
	void SetSomePoperty2(int32 SomePoperty2) override;

	ETbNamesEnum_With_Under_scores GetEnumProperty() const override;
	void SetEnumProperty(ETbNamesEnum_With_Under_scores EnumProperty) override;

	// operations
	void SomeFunction(bool bSomeParam) override;

	void SomeFunction2(bool bSomeParam) override;

	UFUNCTION(BlueprintCallable, Category = "ApiGear|TbNames|NamEs")
	void UseConnection(TScriptInterface<class IApiGearConnection> InConnection);

	/**
	 * Used when the interface client changes subscription status:
	 * either is linked(ready to use) with server side (true) or it is in unlinked state (false).
	 */
	UPROPERTY(BlueprintAssignable, Category = "ApiGear|TbNames|NamEs|Remote", DisplayName = "Subscription Status Changed")
	FApiGearRemoteApiSubscriptionStatusChangedDelegateBP _SubscriptionStatusChangedBP;
	FApiGearRemoteApiSubscriptionStatusChangedDelegate _SubscriptionStatusChanged;

	/**
	 * Informs about the subscription state of the interface client.
	 * @return true if the client is subscribed (plugged in the network) and ready to send and receive messages or false if the server cannot be reached.
	 */
	UFUNCTION(BlueprintCallable, Category = "ApiGear|TbNames|NamEs|Remote")
	bool _IsSubscribed() const;

private:
	void onConnectionStatusChanged(bool bIsConnected);

	/// @brief factory to create the topic map which is used for bindings
	/// @return map with all topics and corresponding function callbacks
	TMap<FString, ApiGear::MQTT::FCallbackFunction> createTopicMap(const FString& ClientId);

	/// @brief sets the value for the property Switch coming from the service
	/// @param InMsg contains the param of the type bool
	void setSwitchLocal(const IApiGearMqttClient::FReceivedMessage& InMsg);

	/// @brief sets the value for the property SomeProperty coming from the service
	/// @param InMsg contains the param of the type int32
	void setSomePropertyLocal(const IApiGearMqttClient::FReceivedMessage& InMsg);

	/// @brief sets the value for the property SomePoperty2 coming from the service
	/// @param InMsg contains the param of the type int32
	void setSomePoperty2Local(const IApiGearMqttClient::FReceivedMessage& InMsg);

	/// @brief sets the value for the property EnumProperty coming from the service
	/// @param InMsg contains the param of the type ETbNamesEnum_With_Under_scores
	void setEnumPropertyLocal(const IApiGearMqttClient::FReceivedMessage& InMsg);

	/// @brief publishes the value for the signal SomeSignal coming from the service
	/// @param InMsg contains the param(s) of the type(s) bool bSomeParam
	void onSomeSignal(const IApiGearMqttClient::FReceivedMessage& InMsg);

	/// @brief publishes the value for the signal SomeSignal2 coming from the service
	/// @param InMsg contains the param(s) of the type(s) bool bSomeParam
	void onSomeSignal2(const IApiGearMqttClient::FReceivedMessage& InMsg);

	/// @brief callback for invoke replies with return value
	/// @param InMsg contains the result payload and MQTT 5 CorrelationData identifying the call
	void onInvokeReply(const IApiGearMqttClient::FReceivedMessage& InMsg);

	// member variable to store the last sent data
	TPimplPtr<TbNamesNamEsPropertiesData> _SentData;
	TScriptInterface<class IApiGearConnection> Connection;
	TMap<FString, ApiGear::MQTT::FCallbackFunction> Topics;

	// subscription readiness tracking
	bool bIsSubscribed = false;
	bool bAnySubscribeFailed = false;
	int32 PendingSubscriptions = 0;
	void DecrementPendingSubscription(bool bSuccess);

	// response handler map for RPC replies with return values
	TMap<FGuid, ApiGear::MQTT::InvokeReplyFunc> ResponseHandlerMap;
	FCriticalSection ResponseHandlerMutex;
	FGuid RegisterResponseHandler(ApiGear::MQTT::InvokeReplyFunc&& Handler);
};
