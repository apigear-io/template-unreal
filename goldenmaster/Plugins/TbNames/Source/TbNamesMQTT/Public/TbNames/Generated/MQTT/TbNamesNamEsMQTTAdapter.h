// SPDX-FileCopyrightText: Copyright ApiGear UG and Epic Games, Inc.
// SPDX-License-Identifier: MIT
// This file has been generated by the ApiGear Code Generator.
// Do not edit. Changes will be lost when the code is re-generated.
#pragma once

#include "TbNames/Generated/api/TbNamesNamEsInterface.h"
#include "Subsystems/GameInstanceSubsystem.h"
#include "ApiGearMQTTTypes.h"
#include "TbNamesNamEsMQTTAdapter.generated.h"

class UApiGearMQTTHost;

DECLARE_LOG_CATEGORY_EXTERN(LogTbNamesNamEsMQTTAdapter, Log, All);

/** @brief handles the adaption between the service implementation and the MQTT protocol
 * takes an object of the type ITbNamesNamEsInterface
 * and publishes state changes via MQTT topics
 */
UCLASS(BlueprintType)
class TBNAMESMQTT_API UTbNamesNamEsMQTTAdapter : public UGameInstanceSubsystem, public ITbNamesNamEsSubscriberInterface
{
	GENERATED_BODY()
public:
	explicit UTbNamesNamEsMQTTAdapter();
	virtual ~UTbNamesNamEsMQTTAdapter() = default;

	// subsystem
	void Initialize(FSubsystemCollectionBase& Collection) override;
	void Deinitialize() override;

	UFUNCTION(BlueprintCallable, Category = "ApiGear|TbNames|NamEs")
	void setBackendService(TScriptInterface<ITbNamesNamEsInterface> InService);

	UFUNCTION(BlueprintCallable, Category = "ApiGear|TbNames|NamEs")
	void setMQTTHost(TSoftObjectPtr<UApiGearMQTTHost> InHost);

private:
	void OnConnectionStatusChanged(bool InIsConnected);

	/// @brief factory to create the topic map which is used for bindings
	/// @return map with all topics and corresponding function callbacks
	TMap<FString, ApiGear::MQTT::FCallbackFunction> createTopicMap();

	// ISubscriberInterface
	void OnSomeSignalSignal(bool bSomeParam) override;

	void OnSomeSignal2Signal(bool bSomeParam) override;

	void OnSwitchChanged(bool bSwitch) override;

	void OnSomePropertyChanged(int32 SomeProperty) override;

	void OnSomePoperty2Changed(int32 SomePoperty2) override;

	void OnEnumPropertyChanged(ETbNamesEnum_With_Under_scores EnumProperty) override;

	// incoming MQTT handlers
	void OnInvokeSomeFunction(const IApiGearMqttClient::FReceivedMessage& InMsg);
	void OnInvokeSomeFunction2(const IApiGearMqttClient::FReceivedMessage& InMsg);
	void OnSetSwitch(const IApiGearMqttClient::FReceivedMessage& InMsg);
	void OnSetSomeProperty(const IApiGearMqttClient::FReceivedMessage& InMsg);
	void OnSetSomePoperty2(const IApiGearMqttClient::FReceivedMessage& InMsg);
	void OnSetEnumProperty(const IApiGearMqttClient::FReceivedMessage& InMsg);

	/** Holds the service backend, can be exchanged with different implementation during runtime */
	UPROPERTY(VisibleAnywhere, Category = "ApiGear|TbNames|NamEs")
	TScriptInterface<ITbNamesNamEsInterface> BackendService;

	TMap<FString, ApiGear::MQTT::FCallbackFunction> Topics;
	bool bIsConnected{false};

	/** The host holding the MQTT connection */
	TSoftObjectPtr<UApiGearMQTTHost> Host;
};
